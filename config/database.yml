# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: db/development.sqlite3

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: db/test.sqlite3


production:
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  database: <%= ENV['defaultdb'] %> # Qui inserisci il nome del tuo database, che hai detto essere 'defaultdb'
  username: <%= ENV['doadmin'] %> # Imposta questa variabile d'ambiente nel tuo ambiente di produzione
  password: <%= ENV['AVNS_z590mtdqWaG1QenvEA4'] %> # Imposta questa variabile d'ambiente nel tuo ambiente di produzione
  host: <%= ENV['db-postgresql-lon1-14464-do-user-16090557-0.c.db.ondigitalocean.com'] %> # Imposta questa variabile d'ambiente nel tuo ambiente di produzione
  port: <%= ENV['25060'] %> # Imposta questa variabile d'ambiente nel tuo ambiente di produzione, che hai detto essere '25060'
  sslmode: require
